# Frontend Environment Variables for CodiesVibe
# Copy this file to .env.local for local development
# This file is safe to commit to git (contains no secrets)

# =============================================================================
# API Configuration
# =============================================================================
# Backend API URL - adjust based on your environment
# Development: http://localhost:4000/api
# Production:  https://api.yourdomain.com/api
VITE_API_URL=http://localhost:4000/api

# =============================================================================
# Environment Configuration
# =============================================================================
# Environment identifier (development, staging, production)
VITE_ENVIRONMENT=development

# Enable debug mode and verbose logging
VITE_DEBUG=true

# Enable development tools (React DevTools, etc.)
VITE_DEV_TOOLS=true

# =============================================================================
# Application Metadata
# =============================================================================
# Application name displayed in the UI
VITE_APP_NAME=CodiesVibe

# Application version (auto-populated during build, or set manually)
VITE_APP_VERSION=development

# =============================================================================
# Development Features (optional)
# =============================================================================
# Enable React DevTools and other development aids
# VITE_REACT_DEVTOOLS=true

# Enable source maps for debugging
# VITE_SOURCE_MAPS=true

# Mock API responses (set to true to use mock data instead of real API)
# VITE_USE_MOCK_API=false

# Flag to indicate local development environment
# VITE_LOCAL_DEV=true

# =============================================================================
# Hot Module Replacement (HMR) Configuration
# =============================================================================
# HMR host (localhost for native dev, 0.0.0.0 for Docker)
VITE_HMR_HOST=localhost

# HMR port for development server
VITE_HMR_PORT=5173

# =============================================================================
# Build Information (Auto-populated during CI/CD)
# =============================================================================
# Build timestamp (set automatically during build process)
# VITE_BUILD_DATE=

# Git commit hash (set automatically during build process)
# VITE_GIT_COMMIT=

# =============================================================================
# Notes
# =============================================================================
# Environment Loading Priority (Vite):
#   1. .env.[mode].local      (e.g., .env.development.local) - NOT committed
#   2. .env.[mode]            (e.g., .env.development) - committed
#   3. .env.local             (loaded in all modes except test) - NOT committed
#   4. .env                   (loaded in all modes) - committed
#
# Quick Setup:
#   Development: cp .env.example .env.local
#   Production:  Use .env.production.example as reference
#
# Important:
#   - VITE_ prefixed variables are embedded at build time and visible in browser
#   - Never include secrets or sensitive data in VITE_ variables
#   - Backend variables go in backend/.env (see backend/.env.example)
#   - Docker variables can be set in docker-compose.override.yml