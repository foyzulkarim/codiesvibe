# CodiesVibe Development Environment Configuration
# Copy this file to .env.dev and update with your development credentials

# Infrastructure Service Credentials (Development)
MONGODB_URI=mongodb://admin:CHANGE_ME_MONGODB_PASSWORD@mongodb:27017/codiesvibe?authSource=admin
REDIS_URL=redis://:CHANGE_ME_REDIS_PASSWORD@redis:6379

# Application Configuration (Development)
NODE_ENV=development
PORT=4001

# GitHub OAuth (Development - use your dev app)
GITHUB_CLIENT_ID=REPLACE_WITH_YOUR_GITHUB_CLIENT_ID
GITHUB_CLIENT_SECRET=REPLACE_WITH_YOUR_GITHUB_CLIENT_SECRET
GITHUB_CALLBACK_URL=http://localhost:4000/api/auth/github/callback

# Security (Development - change for production)
JWT_SECRET=REPLACE_WITH_SECURE_JWT_SECRET_MIN_32_CHARS
COOKIE_SECRET=REPLACE_WITH_SECURE_COOKIE_SECRET_MIN_32_CHARS
CSRF_SECRET=REPLACE_WITH_SECURE_CSRF_SECRET_MIN_32_CHARS

# CORS Configuration (Development)
CORS_ORIGIN=http://localhost:3000

# Rate Limiting (Development - more permissive)
RATE_LIMIT_WINDOW=900000  # 15 minutes
RATE_LIMIT_MAX=1000       # Higher limit for development

# Nginx Gateway Configuration (Development)
NESTJS_SERVICE_HOST=codiesvibe-backend-dev
NESTJS_SERVICE_PORT=4001
FASTIFY_SERVICE_HOST=codiesvibe-fastify-dev
FASTIFY_SERVICE_PORT=4002

# Ollama Configuration (Development)
OLLAMA_BASE_URL=http://host.docker.internal:11434
OLLAMA_MODEL=llama3.2

# MCP Server Configuration (Development)
MCP_SERVER_URL=http://localhost:3001

# Logging Configuration (Development)
LOG_LEVEL=debug

# Build Configuration
BUILD_DATE=$(date -u +'%Y-%m-%dT%H:%M:%SZ')
GIT_COMMIT=$(git rev-parse --short HEAD)
VERSION=development

# Frontend Build Configuration (Development)
VITE_API_URL=http://localhost:4000/api
VITE_APP_VERSION=development
VITE_BUILD_DATE=$(date -u +'%Y-%m-%dT%H:%M:%SZ')
VITE_GIT_COMMIT=$(git rev-parse --short HEAD)