# CI Test Environment Configuration
# This file is used by scripts/test-ci-integration.sh
# DO NOT use in production - these are test-only values

# Server Configuration
NODE_ENV=test
PORT=4003
JWT_SECRET=test-secret

# Database Configuration
MONGODB_URI=mongodb://admin:password123@ci-test-mongodb:27017/codiesvibe?authSource=admin

# Vector Database (Qdrant)
QDRANT_HOST=ci-test-qdrant
QDRANT_PORT=6333

# AI Service (placeholder for CI - actual API not called in health checks)
TOGETHER_API_KEY=test-api-key-for-ci
VLLM_BASE_URL=http://localhost:8000

# Authentication (Clerk - fake keys for CI)
CLERK_SECRET_KEY=sk_test_fake_key_for_ci
CLERK_PUBLISHABLE_KEY=pk_test_fake_key_for_ci

# Feature Flags (disabled for faster CI)
ENABLE_CACHE=false
ENABLE_VECTOR_VALIDATION=false
ENABLE_RATE_LIMITING=false
ENABLE_SECURITY_HEADERS=false
ENSURE_QDRANT_COLLECTIONS=false
