# Frontend Production Environment Variables for CodiesVibe
# Copy this file to .env.production and fill in your actual values
# NEVER commit .env.production to git!

# =============================================================================
# API Configuration
# =============================================================================
# Production API URL (adjust based on your deployment setup)
# For subdomain routing: https://api.yourdomain.com/api
# For path-based routing: https://yourdomain.com/api
VITE_API_URL=https://api.yourdomain.com/api

# =============================================================================
# Build Environment
# =============================================================================
# Node environment for build process
NODE_ENV=production

# =============================================================================
# Application Metadata
# =============================================================================
# Application name displayed in production UI
VITE_APP_NAME=CodiesVibe

# Application version (usually set automatically during CI/CD)
VITE_APP_VERSION=production

# Environment identifier
VITE_ENVIRONMENT=production

# =============================================================================
# Notes
# =============================================================================
# - This file contains ONLY frontend environment variables
# - Backend variables should be configured in backend/.env (see backend/.env.example)
# - VITE_ prefixed variables are embedded at build time and visible in the browser
# - Never include secrets or sensitive data in VITE_ variables
# - For Docker deployments, these can also be set as build args